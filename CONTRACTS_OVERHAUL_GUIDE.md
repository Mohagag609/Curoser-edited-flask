# 📋 دليل الإصلاح الشامل والجذري لنظام العقود والأقساط

## 🎯 نظرة عامة

تم إجراء إصلاح شامل وجذري لنظام العقود والأقساط في المشروع، بهدف إنشاء نظام احترافي وموثوق لإدارة العقود العقارية وأقساطها.

## ✨ المميزات الجديدة

### 1. نموذج العقود المحسّن (Contract Model)
- **حالات العقد**: مسودة، نشط، معلق، مكتمل، ملغي
- **أنواع الدفع**: نقدي، أقساط، مختلط
- **حسابات تلقائية**: خصومات، عمولات، أقساط
- **تتبع شامل**: تواريخ، موافقات، مستخدمين
- **علاقات محسّنة**: مع العملاء، الوحدات، الوسطاء، المشاريع

### 2. نموذج الأقساط المحسّن (Installment Model)
- **حالات القسط**: مستحق، مدفوع، مدفوع جزئياً، متأخر، ملغي
- **أنواع الأقساط**: عادي، دفعة أولى، دفعة سنوية، وديعة صيانة، غرامة تأخير
- **حساب تلقائي للغرامات**: بناءً على أيام التأخير
- **تتبع المدفوعات**: كل دفعة مسجلة بتفاصيلها
- **نظام التذكيرات**: SMS، Email، مكالمات

### 3. واجهات المستخدم الاحترافية
- **قائمة العقود**: 
  - فلاتر متقدمة (حالة، نوع دفع، تواريخ)
  - إحصائيات مباشرة
  - عرض التقدم المالي
  - تصدير البيانات

- **إنشاء عقد جديد**:
  - نموذج تفاعلي مع حسابات تلقائية
  - اقتراح رقم العقد
  - ربط العملاء والوحدات
  - حساب الأقساط المباشر

- **عرض تفاصيل العقد**:
  - معلومات شاملة منظمة
  - شريط تقدم السداد
  - ملخص الأقساط
  - إجراءات سريعة

### 4. نظام الأقساط المتقدم
- **توليد تلقائي للأقساط**: حسب نوع الدفع والفترة
- **أنواع الفترات**: شهري، ربع سنوي، نصف سنوي، سنوي
- **دفعات إضافية**: دفعات سنوية، وديعة صيانة
- **متابعة السداد**: تتبع كل دفعة وحالتها

## 📁 الملفات المضافة/المحدثة

### النماذج (Models)
```
/workspace/acc/models/
├── contract_enhanced.py      # نموذج العقود المحسّن
└── installment_enhanced.py    # نموذج الأقساط المحسّن
```

### المسارات (Routes)
```
/workspace/acc/blueprints/contracts/
└── routes_enhanced.py        # مسارات العقود المحسّنة
```

### القوالب (Templates)
```
/workspace/acc/blueprints/contracts/templates/contracts/
├── index.html               # قائمة العقود
├── create.html              # إنشاء عقد جديد
└── view.html                # عرض تفاصيل العقد
```

### ملفات الدعم
```
/workspace/
├── contracts_installments_overhaul.py    # سكريبت الإصلاح الرئيسي
├── update_contracts_db.py                # تحديث قاعدة البيانات
└── CONTRACTS_OVERHAUL_GUIDE.md          # هذا الدليل
```

## 🚀 خطوات التطبيق

### 1. النسخ الاحتياطي
```bash
# تم إنشاء نسخة احتياطية تلقائياً في:
/workspace/backup_[timestamp]/
```

### 2. تحديث قاعدة البيانات
```bash
python3 update_contracts_db.py
```
هذا السكريبت سيقوم بـ:
- إضافة الأعمدة الجديدة للجداول الموجودة
- إنشاء الجداول الجديدة (الوثائق، المدفوعات، التذكيرات)
- إنشاء الفهارس لتحسين الأداء
- تحديث البيانات الموجودة

### 3. تحديث النماذج
```python
# في /workspace/acc/models/__init__.py
# استبدل:
from acc.models.contract import Contract
from acc.models.installment import Installment

# بـ:
from acc.models.contract_enhanced import Contract, ContractStatus, PaymentType
from acc.models.installment_enhanced import Installment, InstallmentStatus
```

### 4. تحديث المسارات
```python
# في /workspace/acc/blueprints/contracts/__init__.py
# استبدل:
from acc.blueprints.contracts.routes import bp

# بـ:
from acc.blueprints.contracts.routes_enhanced import bp
```

### 5. تحديث ملف التهيئة الرئيسي
```python
# في /workspace/acc/__init__.py
# تأكد من تسجيل blueprints العقود والأقساط
```

## 🔧 التخصيصات المطلوبة

### 1. إعدادات المشروع
- تحديث `config.py` لإضافة إعدادات الغرامات والتذكيرات
- إضافة إعدادات SMS/Email للتذكيرات

### 2. الصلاحيات
- إضافة صلاحيات للموظفين (إنشاء، تعديل، حذف، اعتماد)
- تقييد بعض الإجراءات حسب دور المستخدم

### 3. التقارير
- تقرير العقود النشطة
- تقرير الأقساط المستحقة
- تقرير التحصيلات
- تقرير العمولات

## 📊 مخطط قاعدة البيانات

### جدول العقود (contracts)
```sql
- id (PK)
- project_id (FK)
- code (UNIQUE)
- customer_id (FK)
- unit_id (FK)
- unit_price
- discount_percent
- discount_amount
- final_price
- down_payment
- payment_type
- status
- ... (حقول إضافية)
```

### جدول الأقساط (installments)
```sql
- id (PK)
- contract_id (FK)
- installment_number
- amount
- paid_amount
- due_date
- status
- type
- ... (حقول إضافية)
```

### جداول جديدة
- **contract_documents**: وثائق العقود
- **installment_payments**: مدفوعات الأقساط
- **installment_reminders**: تذكيرات الأقساط

## 🧪 الاختبار

### 1. اختبار إنشاء عقد
- إنشاء عقد نقدي
- إنشاء عقد بالأقساط
- التحقق من توليد الأقساط

### 2. اختبار العمليات المالية
- إضافة دفعة
- حساب الغرامات
- تحديث حالة الأقساط

### 3. اختبار التقارير
- كشف حساب العميل
- تقرير الأقساط المستحقة
- تقرير العمولات

## 🚨 ملاحظات مهمة

1. **النسخ الاحتياطي**: احتفظ بنسخة احتياطية قبل تطبيق التغييرات
2. **البيانات القديمة**: تم الحفاظ على التوافق مع البيانات الموجودة
3. **الأداء**: تم إضافة فهارس لتحسين سرعة الاستعلامات
4. **الأمان**: تحقق من الصلاحيات قبل السماح بالعمليات الحساسة

## 📈 التحسينات المستقبلية

1. **نظام الموافقات**: workflow للموافقة على العقود
2. **التكامل المالي**: ربط مع أنظمة المحاسبة
3. **البوابة الإلكترونية**: للعملاء لمتابعة عقودهم
4. **التنبيهات الذكية**: تنبيهات تلقائية للأقساط
5. **التقارير المتقدمة**: dashboards تفاعلية

## 🤝 الدعم

في حالة وجود أي مشاكل أو استفسارات:
1. راجع السجلات في `/workspace/logs/`
2. تحقق من النسخة الاحتياطية
3. راجع هذا الدليل للتأكد من اتباع جميع الخطوات

---

**تم الإنشاء بتاريخ**: {{ datetime.now() }}
**الإصدار**: 1.0
**المطور**: نظام AI المساعد